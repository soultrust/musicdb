{
  // Solution-style root: the editor loads `tsconfig.app.json` (app/Vite) and `tsconfig.test.json` (Vitest, jest-dom) so `*.test.tsx` typecheck correctly.
  "files": [],
  "references": [{ "path": "./tsconfig.app.json" }, { "path": "./tsconfig.test.json" }]
}
